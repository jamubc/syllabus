# Prevent npm from looking up the directory tree for lockfiles
lockfile-version=3
save-exact=false
package-lock=true

# Ensure this directory is treated as a standalone project
prefer-workspace-packages=false